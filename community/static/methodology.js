const language=document.getElementById('language');language.value=new URLSearchParams(location.search).get('lang')||localStorage.getItem('ha-benchmark-language')||'de';
function render(){const de=language.value==='de';document.documentElement.lang=language.value;document.getElementById('method').innerHTML=de?`
<h2>Was der Index aussagt</h2><p>Green = 100. Höhere Indizes sind besser. Der Benchmark misst definierte Mikro-Workloads, nicht die Geschwindigkeit jeder Automation. Core 2026.9.1 läuft isoliert im Add-on; nur die API-Messung fragt deine laufende Home-Assistant-Instanz ab.</p>
<h2>Gewichte</h2><table><tr><th>Kategorie</th><th>Anteil</th></tr><tr><td>Core Events</td><td>20 %</td></tr><tr><td>State Changes</td><td>20 %</td></tr><tr><td>Entity-Filter</td><td>10 %</td></tr><tr><td>Entity-IDs</td><td>5 %</td></tr><tr><td>JSON States</td><td>15 %</td></tr><tr><td>Recorder-Speicher</td><td>20 %</td></tr><tr><td>API-Latenz</td><td>10 %</td></tr></table>
<p>Die Gewichte sind eine dokumentierte Designentscheidung, keine empirisch bewiesenen Anteile typischer HA-Nutzung. Sie werden nicht nach Hersteller oder gewünschtem Ranking verändert.</p>
<h2>Berechnung</h2><p>Durchsatzindex = 100 × Messwert / Green-Referenz. Latenzindex = 100 × Green-Referenz / Messwert. Gesamtindex = exp(Σ Gewicht × ln(Kategorieindex)). Die Implementierung rundet Kategorieindizes vor der Gesamtberechnung. Ein fehlgeschlagener Test macht den Lauf für die Community ungültig.</p>
<h2>Recorder-Speicher</h2><p>Temporäre SQLite-Datenbank im Add-on-Datenverzeichnis, WAL und synchronous=FULL. Schreiben 25 %, Commit-p95 40 %, Random-Read-p95 20 %, Checkpoint 15 % innerhalb der 20-%-Speicherkategorie. Schreibdurchsatz umfasst Datenerzeugung und Commits, nicht den anschließenden Checkpoint. p95 bedeutet: 95 % der Zugriffe waren höchstens so langsam.</p><p>Vor dem Lesen werden Datenbankhandles geschlossen und Dateicache-Freigabe angefordert. Das garantiert keine physisch kalten Zugriffe. Gemessen wird das App-Datenlaufwerk, nicht zwingend der Speicher einer externen Recorder-Datenbank. FULL fordert dauerhafte Synchronisation an; Hardware kann diese unterschiedlich umsetzen. Kein Stromausfall-, Alterungs- oder Haltbarkeitstest.</p>
<h2>Profile</h2><p>Light: 6 MiB Nutzdaten, 100 Commits, 1.000 Lesezugriffe. Full: 64 MiB, 1.000 Commits, 5.000 Lesezugriffe. Auf dem Referenzgerät wurden etwa 15 bzw. 197 MiB temporärer Platzbedarf vor dem Checkpoint beobachtet. Das ist kein garantiertes Maximum. Dateien werden nach dem Test entfernt. Light erzeugt ebenfalls Last; Full nur auf Testgeräten.</p>
<h2>Kalibrierung und Vergleichbarkeit</h2><p>CORE-2026.9.1-R3 · GREEN-CORE-2026-09-C. Sechs Light- und sechs Full-Läufe eines als Green angegebenen Geräts: HAOS 18.2, HA Core 2026.9.1. Der Median jeder Teilmessung bildet die Referenz. Ein belasteter früher Light-Lauf wurde ausgeschlossen. Speicherart war im Export nicht angegeben. Dies ist eine vorläufige Ein-Gerät-Referenz, keine Flottenkalibrierung.</p><p>Ranglisten trennen Profil und Benchmark-Version. 0.4.1 und 0.5.0 nutzen dieselbe Messmethodik und Referenz, werden aber getrennt angezeigt. Ein 3-Lauf-Eintrag verwendet den Median der drei Gesamtindizes; Kategorien werden aus medianen Rohwerten berechnet. Er ist wiederholter, nicht unabhängig verifizierter Community-Input. Temperatur/Energie haben 0 % Gewicht.</p>
<p>Vor Messungen Leerlauf abwarten, keine Backups/Updates ausführen und gleiche Bedingungen dokumentieren. Hardwareangaben und Messungen können manipuliert sein. Serverseitige Plausibilitätsprüfung ist keine Hardware-Attestierung.</p>`:`
<h2>What the index means</h2><p>Green = 100. Higher indices are better. This benchmark measures defined micro-workloads, not the speed of every automation. Core 2026.9.1 runs isolated inside the add-on; only the API test queries your running Home Assistant instance.</p>
<h2>Weights</h2><table><tr><th>Category</th><th>Weight</th></tr><tr><td>Core Events</td><td>20%</td></tr><tr><td>State Changes</td><td>20%</td></tr><tr><td>Entity filters</td><td>10%</td></tr><tr><td>Entity IDs</td><td>5%</td></tr><tr><td>JSON States</td><td>15%</td></tr><tr><td>Recorder storage</td><td>20%</td></tr><tr><td>API latency</td><td>10%</td></tr></table><p>Weights are a documented design choice, not empirically proven shares of typical HA use. They are not adjusted by manufacturer or desired ranking.</p>
<h2>Calculation</h2><p>Throughput index = 100 × measured value / Green reference. Latency index = 100 × Green reference / measured value. Overall index = exp(Σ weight × ln(category index)). Category indices are rounded before the overall calculation. A failed test makes a run ineligible for publication.</p>
<h2>Recorder storage</h2><p>Temporary SQLite database in the add-on data directory; WAL with synchronous=FULL. Subweights: write 25%, commit p95 40%, random read p95 20%, checkpoint 15%, within the storage category's 20% overall weight. Write throughput includes row creation and commits, but not the subsequent checkpoint. p95 means 95% of operations took no longer.</p><p>Database handles are closed and file-cache eviction is requested before reading. Physically cold reads are not guaranteed. This tests the add-on data drive, not necessarily an external recorder database. FULL requests durable synchronization; hardware implementations vary. This is not a power-loss, ageing or endurance test.</p>
<h2>Profiles</h2><p>Light: 6 MiB payload, 100 commits, 1,000 reads. Full: 64 MiB, 1,000 commits, 5,000 reads. The reference device used approximately 15/197 MiB temporary space before checkpoint; this is not a guaranteed maximum. Files are removed afterwards. Light still creates load; Full is for test devices only.</p>
<h2>Calibration and comparisons</h2><p>CORE-2026.9.1-R3 · GREEN-CORE-2026-09-C. Six Light and six Full runs on a device reported as Green, HAOS 18.2 and HA Core 2026.9.1. Each metric's median defines the reference. One early loaded Light run was excluded. Storage type was unspecified in the export. This is a provisional single-device reference, not fleet calibration.</p><p>Rankings separate profile and benchmark version. 0.4.1 and 0.5.0 use the same measurement method and reference but are listed separately. A 3-run entry uses the median of its three overall scores; category indices use median raw values. It represents repeated, not independently verified community input. Temperature and energy carry 0% weight.</p><p>Let the system idle, avoid backups/updates and document equal conditions. Hardware descriptions and results can be manipulated. Server-side validation does not attest physical hardware.</p>`;}
language.onchange=render;render();
