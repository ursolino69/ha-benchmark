'use strict';
const $=id=>document.getElementById(id), community=document.body.dataset.mode==='community';
let lang=localStorage.getItem('ha-benchmark-language')||(navigator.language.startsWith('de')?'de':'en');
const t=(de,en)=>lang==='de'?de:en, esc=v=>String(v??'—').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
let rows=[],latest=null,selected=new Set(),offset=0,pollTimer=null;
const weights={core_events:20,state_changes:20,entity_filter:10,entity_validation:5,json_states:15,sqlite:20,api_latency:10};
const info={
 core_events:['Core Events','Core Events','Interner Event-Bus: Erzeugen und Verarbeiten von Ereignissen. Höherer Durchsatz ist besser. Relevant für Sensorereignisse und Automationstrigger.','Internal event bus: creating and processing events. Higher throughput is better. Relevant to sensor events and automation triggers.'],
 state_changes:['State Changes','State Changes','Zustandsereignisse mit Entity-Listenern. Höher ist besser. Relevant bei vielen Entitäten und zustandsabhängigen Automationen.','State events with entity listeners. Higher is better. Relevant to many entities and state-driven automations.'],
 entity_filter:['Entity-Filter','Entity filters','Include-/Exclude-Filter für Entitäten und Domains. Höher ist besser. Relevant für Recorder- und Logbuchfilter.','Include/exclude filters for entities and domains. Higher is better. Relevant to recorder and logbook filters.'],
 entity_validation:['Entity-IDs','Entity IDs','Validierung von Entity-IDs; kleine Core-Operation. Höher ist besser. Zeigt einen Teil der Python-Grundleistung, nicht die gesamte Automationsleistung.','Entity ID validation; a small Core operation. Higher is better. Indicates part of Python performance, not total automation performance.'],
 json_states:['JSON States','JSON states','Serialisierung von State-Objekten. Höher ist besser. Relevant für große API- und WebSocket-Antworten und viele Dashboard-Zustände.','Serialization of State objects. Higher is better. Relevant to large API/WebSocket responses and many dashboard states.'],
 sqlite:['Recorder-Speicher','Recorder storage','SQLite mit WAL und FULL-Synchronisierung. Teilgewichte: Schreiben 25 %, Commit-p95 40 %, Lesen-p95 20 %, Checkpoint 15 %. Höherer Durchsatz und niedrigere Latenzen sind besser. Relevant für Recorder und Verlauf. p95: 95 % der Zugriffe waren höchstens so langsam. Cache-Freigabe ist nur ein Versuch; kein Haltbarkeitstest.','SQLite with WAL and FULL synchronization. Subweights: write 25%, commit p95 40%, read p95 20%, checkpoint 15%. Higher throughput and lower latency are better. Relevant to recorder and history. p95: 95% of operations took no longer than this value. Cache eviction is best effort; this is not an endurance test.'],
 api_latency:['API-Latenz','API latency','Mediane Antwortzeit der laufenden HA-API. Niedriger ist besser. Enthält Supervisor-/HTTP-Overhead und Hintergrundlast. Relevant für API-Bedienung; misst keine Funklatenz.','Median response time of the running HA API. Lower is better. Includes Supervisor/HTTP overhead and background load. Relevant to API interactions; does not measure radio latency.']};
async function api(path,body,method='POST'){const r=await fetch('api/'+path,body===undefined?{}:{method,headers:{'Content-Type':'application/json','X-Benchmark-Request':'1'},body:JSON.stringify(body)});const d=await r.json();if(!r.ok)throw Error(d.error||String(r.status));return d}
function message(e){$('message').textContent=e.message||e}
function modal(title,html){$('dialogTitle').textContent=title;$('dialogBody').innerHTML=html;$('dialog').showModal()}
function helpMetric(k,v){const a=info[k];modal(t(a[0],a[1]),`<p>${esc(t(a[2],a[3]))}</p><p>${t('Gewicht im Gesamtindex','Weight in overall index')}: ${weights[k]} %</p><pre>${esc(JSON.stringify(v,null,2))}</pre>`)}
function shell(){document.documentElement.lang=lang;document.title='HA Benchmark';document.body.innerHTML=`<main><header><h1>HA Benchmark</h1><nav><a href="methodology.html?lang=${lang}" target="_blank" rel="noopener">${t('Methodik','Methodology')} R3</a><select id="language" aria-label="Language"><option value="de">Deutsch</option><option value="en">English</option></select><span class="muted">0.5.0</span></nav></header><div id="message" role="status"></div><div id="content"></div><footer><img class="brand" src="brand.png" alt="Smartdomo"><span>${t('Green = 100 · Kalibrierung C','Green = 100 · Calibration C')}</span>${community?`<a href="privacy.html">${t('Datenschutz und Impressum','Privacy and legal notice')}</a><a href="admin.html">${t('Moderation','Moderation')}</a>`:`<a href="https://benchmark.smartdomo.de" target="_blank" rel="noopener">Share & Compare</a>`}</footer></main><dialog id="dialog" aria-labelledby="dialogTitle"><h2 id="dialogTitle"></h2><div id="dialogBody"></div><form method="dialog"><button>${t('Schließen','Close')}</button></form></dialog>`;$('language').value=lang;$('language').onchange=e=>{lang=e.target.value;localStorage.setItem('ha-benchmark-language',lang);selected.clear();clearTimeout(pollTimer);start()}}
function renderResult(r){latest=r;if(!r)return;$('result').innerHTML=`<div class="panel"><span>${t('Gesamtindex','Overall index')}</span><div class="score">${esc(r.index)}</div><small>${r.index!=null?'Green = 100':t('Keine kompatible Kalibrierung','No compatible calibration')}</small></div><h2>${t('Kategorien','Categories')}</h2><div class="grid">${Object.entries(r.tests||{}).filter(([k])=>info[k]).map(([k,v])=>`<button class="metric" data-metric="${k}"><span class="weight">${weights[k]} %</span>${esc(t(info[k][0],info[k][1]))}<b>${esc(v.index??r.indices?.[k]??v.value)}</b><small>${v.index!=null||r.indices?.[k]!=null?'Index':esc(v.unit||'')}</small><small>${t('Erklärung','Explanation')}</small></button>`).join('')}</div><h2>${t('Temperatur und Energie','Temperature and energy')}</h2><div class="grid" id="environment"></div><h2>${t('System','System')}</h2><div class="panel"><dl>${Object.entries(r.system||{}).filter(([k])=>!['platform'].includes(k)).map(([k,v])=>`<dt>${esc(systemLabel(k))}</dt><dd>${esc(v)}</dd>`).join('')}</dl></div>`;
document.querySelectorAll('[data-metric]').forEach(b=>b.onclick=()=>helpMetric(b.dataset.metric,r.tests[b.dataset.metric]));renderEnvironment(r.environment||r.runs?.[0]?.environment||{});}
function systemLabel(k){const names={device_name:['Gerätename (lokal)','Device name (local)'],machine:['Maschinentyp','Machine type'],cpu_model:['Prozessor','Processor'],logical_cpus:['Logische Kerne','Logical CPUs'],memory_total_mib:['RAM (MiB)','RAM (MiB)'],storage_type:['Speicherart','Storage type'],storage_size_gb:['Speichergröße (GB)','Storage size (GB)'],architecture:['Architektur','Architecture'],home_assistant:['HA Core','HA Core'],operating_system:['HA OS','HA OS'],supervisor:['Supervisor','Supervisor'],benchmark_engine_core:['Benchmark Core','Benchmark Core']};return names[k]?t(...names[k]):k}
function renderEnvironment(e){const temp=e.temperature||{},power=e.energy||{},psi=e.pressure?.end||{};
const items=[
 [t('Starttemperatur','Start temperature'),temp.start,'°C',t('Erster erfasster Temperaturwert. Quelle: Sysfs oder konfigurierte HA-Entität. Hilft, unterschiedliche Startbedingungen zu erkennen.','First sampled temperature from sysfs or the configured HA entity. Helps identify different starting conditions.')],
 [t('Maximaltemperatur','Peak temperature'),temp.maximum,'°C',t('Höchster abgetasteter Wert, kein garantierter absoluter Spitzenwert. Kühlung und Umgebung beeinflussen ihn; allein kein Nachweis von Drosselung.','Highest sampled value, not a guaranteed absolute peak. Cooling and ambient temperature affect it; it does not alone prove throttling.')],
 [t('Mittlere Leistung','Average power'),power.average_w,'W',t('Mittelwert der Sensorwerte während des Laufs. Ein Steckdosenmesser erfasst gewöhnlich auch Netzteilverluste. Kurze Tests und träge Sensoren begrenzen die Aussagekraft.','Average sensor readings during the run. A wall meter typically includes power supply losses. Short tests and slow sensors limit accuracy.')],
 [t('Spitzenleistung','Peak power'),power.peak_w,'W',t('Höchster gemessener Leistungswert. Zwischen Abtastungen liegende Spitzen können fehlen.','Highest sampled power value. Peaks between samples may be missed.')],
 [t('Energie im Test','Energy during test'),power.consumption_wh,'Wh',t('Aus Leistung und Zeit zwischen verfügbaren Messpunkten integriert. Kein Jahresverbrauch; bei wenigen Punkten ungenau.','Integrated power over time between available samples. Not annual consumption; inaccurate with few samples.')],
 ...['cpu','memory','io'].map(k=>[`${k.toUpperCase()} PSI`,psi[k]?.avg10,'%',t('Zeitanteil mit Ressourcendruck im 10-Sekunden-Mittel am Testende. Der Benchmark selbst kann Druck verursachen. Kein Prozentwert der CPU-Auslastung.','Share of time with resource pressure in the 10-second average at the end. The benchmark itself can cause pressure. Not CPU utilization.')])];
$('environment').innerHTML=items.map(([name,value,unit],i)=>`<button class="metric" data-env="${i}"><span class="weight">0 %</span>${name}<b>${value==null?'—':esc(value)}</b><small>${value==null?t('Nicht verfügbar','Unavailable'):unit}</small></button>`).join('');document.querySelectorAll('[data-env]').forEach(b=>b.onclick=()=>{const i=items[+b.dataset.env];modal(i[0],`<p>${i[3]}</p><p>${t('Diagnosewert. Fließt mit 0 % in den Index ein.','Diagnostic only. Weight in the index: 0%.')}</p>`)});}
async function localHistory(){rows=await api('results');$('history').innerHTML=rows.map(r=>`<tr><td><input type="checkbox" aria-label="${esc(r.result_id)}" data-select="${r.result_id}" ${selected.has(r.result_id)?'checked':''}></td><td><button data-result="${r.result_id}">${esc(new Date(r.finished_at).toLocaleString())}</button></td><td>${esc(r.profile)}</td><td>${esc(r.benchmark_version)}</td><td>${esc(r.index)}</td></tr>`).join('');document.querySelectorAll('[data-select]').forEach(b=>b.onchange=()=>b.checked?selected.add(b.dataset.select):selected.delete(b.dataset.select));document.querySelectorAll('[data-result]').forEach(b=>b.onclick=()=>renderResult(rows.find(r=>r.result_id===b.dataset.result)));if(rows.length)renderResult(rows[0]);}
async function poll(){try{const s=await api('status');$('progress').value=s.progress;$('status').textContent=s.running?t('Messung läuft','Benchmark running')+` · ${s.progress} %`:t('Bereit','Ready');$('light').disabled=s.running;$('full').disabled=s.running||!s.full_allowed;$('cancel').disabled=!s.running;if(s.error)message(s.error);if(s.running)pollTimer=setTimeout(poll,1000);else await localHistory()}catch(e){message(e)}}
async function run(profile){if(profile==='full'&&!confirm(t('Full nur auf Testgeräten starten. Fortfahren?','Full is for test devices only. Continue?')))return;try{await api('run',{profile});poll()}catch(e){message(e)}}
async function sharing(){const ids=[...selected];if(![1,3].includes(ids.length)){message(t('Im Verlauf einen oder drei Läufe auswählen.','Select one or three runs in history.'));return}modal('Share & Compare',`<p>${t('Diese Messungen werden öffentlich. Keine privaten Namen in Alias oder Gerätemodell eintragen.','These measurements will be public. Do not put private names into the alias or device model.')}</p><label>${t('Öffentliches Gerätemodell','Public device model')}<br><input id="deviceModel" maxlength="80" placeholder="Home Assistant Green"></label><label>Alias (${t('optional','optional')})<br><input id="alias" maxlength="40"></label><label><input id="includeEnv" type="checkbox"> ${t('Temperatur und Energie mit veröffentlichen','Include temperature and energy')}</label><button id="previewButton">${t('Datenvorschau','Preview data')}</button><pre id="preview" class="hidden"></pre><div id="shareError" role="status"></div><div id="consentBox" class="hidden"><p><a href="https://benchmark.smartdomo.de/privacy.html" target="_blank" rel="noopener">${t('Datenschutz','Privacy')}</a></p><label><input type="checkbox" id="consent"> ${t('Ich stimme der öffentlichen Veröffentlichung dieser Daten zu.','I agree to publishing these data publicly.')}</label><button id="publish" class="primary">${t('Jetzt veröffentlichen','Publish now')}</button></div>`);
let draft=null;const invalidate=()=>{draft=null;$('consentBox').classList.add('hidden');$('preview').classList.add('hidden')};['deviceModel','alias','includeEnv'].forEach(id=>$(id).oninput=invalidate);
$('previewButton').onclick=async()=>{try{if(!$('deviceModel').value.trim())throw Error(t('Gerätemodell fehlt','Device model required'));draft={ids,device_model:$('deviceModel').value,alias:$('alias').value,include_environment:$('includeEnv').checked};const p=await api('share-preview',draft);$('preview').textContent=JSON.stringify(p,null,2);$('preview').classList.remove('hidden');$('consentBox').classList.remove('hidden');$('shareError').textContent=''}catch(e){$('shareError').textContent=e.message}};
$('publish').onclick=async()=>{if(!draft||!$('consent').checked)return;$('publish').disabled=true;try{const r=await api('share',{...draft,consent:true});showKey(r)}catch(e){$('shareError').textContent=e.message;$('publish').disabled=false}};}
function showKey(r){$('dialogBody').innerHTML=`<p><a href="${esc(r.url)}" target="_blank" rel="noopener">${t('Ergebnis öffnen','Open result')}</a></p><p>${t('Privater Löschschlüssel. Nicht zusammen mit dem Ergebnislink teilen. Er bleibt zusätzlich im Add-on gespeichert.','Private deletion key. Do not share it with the result link. It is also saved in the add-on.')}</p><pre>${esc(r.delete_key)}</pre><button id="downloadKey">${t('Schlüssel speichern','Save key')}</button>`;$('downloadKey').onclick=()=>{const a=document.createElement('a');a.href=URL.createObjectURL(new Blob([JSON.stringify(r,null,2)],{type:'application/json'}));a.download='benchmark-private-key-'+r.id+'.json';a.click();setTimeout(()=>URL.revokeObjectURL(a.href),1000)}}
async function savedKeys(){try{const list=await api('shared');modal(t('Veröffentlichungen und private Schlüssel','Publications and private keys'),list.map((r,i)=>`<p><button data-key="${i}">${esc(r.id)}</button></p>`).join('')||t('Keine Veröffentlichungen','No publications'));document.querySelectorAll('[data-key]').forEach(b=>b.onclick=()=>showKey(list[+b.dataset.key]))}catch(e){message(e)}}
async function addon(){ $('content').innerHTML=`<section class="grid"><div class="panel"><h2>Light</h2><p>${t('Begrenzte Last. 6 MiB Nutzdaten, 100 synchronisierte Commits. Auch auf Produktivsystemen nur in ruhigen Phasen starten.','Limited load. 6 MiB payload, 100 synchronized commits. On production systems, run only during quiet periods.')}</p><button id="light" class="primary">${t('Light starten','Run Light')}</button></div><div class="panel"><h2>Full</h2><p>${t('Nur Testgeräte. 64 MiB Nutzdaten, 1.000 synchronisierte Commits. Freigabe in der App-Konfiguration erforderlich.','Test devices only. 64 MiB payload, 1,000 synchronized commits. Enable in app configuration.')}</p><button id="full">${t('Full starten','Run Full')}</button></div></section><progress id="progress" max="100" value="0"></progress><div class="toolbar"><span id="status"></span><button id="cancel">${t('Abbrechen','Cancel')}</button></div><section id="result"></section><h2>${t('Verlauf','History')}</h2><p>${t('Zum Teilen einen oder drei kompatible Läufe auswählen.','Select one or three compatible runs to share.')}</p><div class="table-wrap"><table><thead><tr><th></th><th>${t('Zeitpunkt','Time')}</th><th>${t('Profil','Profile')}</th><th>Version</th><th>Index</th></tr></thead><tbody id="history"></tbody></table></div><div class="toolbar"><button id="share" class="primary">Share & Compare</button><button id="keys">${t('Meine Veröffentlichungen','My publications')}</button><a href="api/export.json">JSON</a><a href="api/export.csv">CSV</a></div>`;$('light').onclick=()=>run('light');$('full').onclick=()=>run('full');$('cancel').onclick=async()=>{try{await api('cancel',{})}catch(e){message(e)}};$('share').onclick=sharing;$('keys').onclick=savedKeys;await poll();}
async function ranking(){const profile=$('profile').value,version=$('version').value;const d=await api(`entries?profile=${profile}&version=${version}&offset=${offset}`);rows=d.entries;selected.clear();$('ranking').innerHTML=rows.map((r,i)=>`<tr><td><input type="checkbox" aria-label="${esc(r.device_model)}" data-compare="${r.id}"></td><td>${offset+i+1}</td><td><a href="?result=${r.id}">${esc(r.device_model)}</a><br><small>${esc(r.alias)}</small></td><td><b>${esc(r.index)}</b></td><td>${r.count===3?`<span class="badge" title="${t('Median aus drei Läufen; nicht unabhängig verifiziert','Median of three runs; not independently verified')}">▤ ${t('3-Lauf-Median','3-run median')}</span>`:t('Einzellauf','Single run')}</td><td>${esc(r.system.storage_type)} · ${esc(r.system.memory_total_mib)} MiB</td></tr>`).join('')||`<tr><td colspan="6">${t('Noch keine Ergebnisse für diese Auswahl.','No results for this selection yet.')}</td></tr>`;document.querySelectorAll('[data-compare]').forEach(b=>b.onchange=()=>b.checked?selected.add(b.dataset.compare):selected.delete(b.dataset.compare));$('previous').disabled=offset===0;$('next').disabled=rows.length<100;}
function compare(){const picks=rows.filter(r=>selected.has(r.id));if(picks.length<2||picks.length>4){message(t('Zwei bis vier Geräte auswählen.','Select two to four devices.'));return}modal(t('Gerätevergleich','Device comparison'),`<div class="table-wrap"><table><tr><th></th>${picks.map(r=>`<th>${esc(r.device_model)}</th>`).join('')}</tr>${['index',...Object.keys(weights)].map(k=>`<tr><th>${k==='index'?t('Gesamtindex','Overall index'):esc(t(info[k][0],info[k][1]))}</th>${picks.map(r=>`<td>${esc(k==='index'?r.index:r.indices[k])}</td>`).join('')}</tr>`).join('')}</table></div><p>${t('Green = 100. Mikromessungen sagen nicht die Geschwindigkeit jeder realen Installation voraus.','Green = 100. Microbenchmarks do not predict the speed of every real installation.')}</p>`)}
async function publicPage(){const id=new URLSearchParams(location.search).get('result');if(id){const r=await api('entries/'+encodeURIComponent(id));$('content').innerHTML=`<p><a href="./">${t('Zur Rangliste','Back to ranking')}</a></p><h2>${esc(r.device_model)}</h2><p>${esc(r.alias)} · ${esc(r.profile)} · ${r.count===3?t('▤ 3-Lauf-Median','▤ 3-run median'):t('Einzellauf','Single run')} · ${t('Nicht unabhängig verifiziert','Not independently verified')}</p><section id="result"></section><p>${t('Bei Dreiermessungen sind veröffentlichte Umweltwerte Mediane der verfügbaren Messwerte. Alle Rohdaten stehen im JSON.','For three-run entries, published environment values are medians of available measurements. JSON contains all raw data.')}</p><div class="toolbar"><a href="api/entries/${esc(r.id)}">JSON</a><button id="delete">${t('Eigene Veröffentlichung löschen','Delete your publication')}</button></div>`;renderResult(r);$('delete').onclick=()=>{modal(t('Veröffentlichung löschen','Delete publication'),`<label>${t('Privater Löschschlüssel','Private deletion key')}<input id="deleteKey" type="password" autocomplete="off"></label><p>${t('Die Veröffentlichung wird unwiderruflich entfernt. Lokale Messungen bleiben erhalten.','The publication will be permanently removed. Local measurements remain.')}</p><button id="deleteNow">${t('Endgültig löschen','Delete permanently')}</button><p id="deleteError"></p>`);$('deleteNow').onclick=async()=>{try{await api('entries/'+r.id,{delete_key:$('deleteKey').value},'DELETE');location.href='./'}catch(e){$('deleteError').textContent=e.message}}};return}
$('content').innerHTML=`<h2>Share & Compare</h2><p>${t('Community-Messungen · nicht unabhängig verifiziert. Teilen erfolgt in der HA-Benchmark-App.','Community measurements · not independently verified. Publish from the HA Benchmark app.')}</p><div class="toolbar"><label>${t('Profil','Profile')} <select id="profile"><option>light</option><option>full</option></select></label><label>Version <select id="version"><option>0.5.0</option><option>0.4.1</option></select></label><button id="compare">${t('Auswahl vergleichen','Compare selected')}</button></div><div class="table-wrap"><table><thead><tr><th></th><th>#</th><th>${t('Gerät / Alias','Device / alias')}</th><th>Index</th><th>${t('Messreihe','Samples')}</th><th>${t('Speicher','Memory / storage')}</th></tr></thead><tbody id="ranking"></tbody></table></div><div class="toolbar"><button id="previous">${t('Zurück','Previous')}</button><button id="next">${t('Weiter','Next')}</button></div>`;for(const id of ['profile','version'])$(id).onchange=()=>{offset=0;ranking().catch(message)};$('compare').onclick=compare;$('previous').onclick=()=>{offset=Math.max(0,offset-100);ranking().catch(message)};$('next').onclick=()=>{offset+=100;ranking().catch(message)};await ranking();}
async function start(){shell();try{if(community)await publicPage();else await addon()}catch(e){message(e)}}
start();
