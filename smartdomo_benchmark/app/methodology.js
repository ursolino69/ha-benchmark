'use strict';
const language=document.getElementById('language'),queryLanguage=new URLSearchParams(location.search).get('lang'),saved=localStorage.getItem('ha-benchmark-language'),browser=(navigator.languages?.[0]||navigator.language||'en').toLowerCase();
language.value=['de','en'].includes(queryLanguage)?queryLanguage:['de','en'].includes(saved)?saved:browser.startsWith('de')?'de':'en';
function theme(){document.documentElement.dataset.theme=matchMedia('(prefers-color-scheme:dark)').matches?'dark':'light'}theme();matchMedia('(prefers-color-scheme:dark)').addEventListener('change',theme);
const weights=`<div class="table-wrap"><table><thead><tr><th>Category / Kategorie</th><th>Weight / Anteil</th></tr></thead><tbody><tr><td>Core events</td><td>15 %</td></tr><tr><td>State changes</td><td>20 %</td></tr><tr><td>Entity processing</td><td>5 %</td></tr><tr><td>Fresh JSON states</td><td>10 %</td></tr><tr><td>Recorder storage</td><td>20 %</td></tr><tr><td>API latency</td><td>10 %</td></tr><tr><td>Parallel Core load</td><td>20 %</td></tr></tbody></table></div>`;
const de=`<h1>Benchmark-Methodik R4</h1><h2>Status</h2><p>R4 verändert die Arbeitslasten grundlegend und ist deshalb nicht mit R3 vergleichbar. Bis neue Green-Referenzserien für Light und Full vorliegen, zeigt die App ausschließlich Rohwerte. R3-Werte werden bewusst nicht umgerechnet oder übernommen.</p><h2>Ziel der Überarbeitung</h2><p>R4 misst weiterhin definierte Leistung statt allgemeiner Eignung. Die Arbeitsmengen orientieren sich nun an Installationen mit hunderten Entitäten und kombinieren wiederkehrende mit neuen Daten. So beeinflussen reine Python-Cache-Treffer das Ergebnis deutlich weniger. Gerätepreis, Marktverbreitung und Eignungsklassen sind nicht Bestandteil von R4.</p><h2>Vorgesehene Gewichtung</h2>${weights}<p>Diese Gewichte werden erst nach erfolgreicher Kalibrierung zur Indexberechnung verwendet.</p><h2>Core Events</h2><p>Acht Ereignistypen werden mit variierenden Nutzdaten in begrenzten Bursts erzeugt und vollständig verarbeitet. Light verwendet 24.000, Full 120.000 Events. Erzeugung und Abarbeitung liegen innerhalb der Messzeit.</p><h2>State Changes</h2><p>Die Zustandsänderungen verteilen sich auf 400 Entitäten im Light- und 1.000 Entitäten im Full-Profil. Jedes Ereignis erhält neue alte und neue Home-Assistant-State-Objekte mit wechselnden Werten und Attributen. Alle registrierten Entity-Listener werden tatsächlich angesprochen; die Verarbeitung erfolgt in speicherbegrenzten Bursts.</p><h2>Entity-Verarbeitung und JSON</h2><p>Entity-Verarbeitung kombiniert Include-/Exclude-Filter und ID-Prüfung. 80 % der IDs stammen aus einem wiederkehrenden Arbeitsbestand, 20 % sind neu. JSON erzeugt in jedem Durchlauf neue State-Objekte und Attribute. Wiederholtes Serialisieren desselben Objekts entfällt.</p><h2>Parallele Core-Last</h2><p>Bis zu zwei Worker im Light- und vier Worker im Full-Profil erzeugen und serialisieren gleichzeitig frische State-Objekte in getrennten Prozessen. Prozessstart und Modulimport liegen außerhalb der Messzeit. Diese Kategorie bildet Mehrkern-Kapazität für gleichzeitig laufende HA-nahe Aufgaben und Apps ab; sie behauptet nicht, dass der ereignisgesteuerte Core selbst multithreaded arbeitet.</p><h2>Recorder-Speicher und API</h2><p>Der SQLite-Test bleibt WAL-basiert mit synchronous=FULL. Innerhalb der Speicherkategorie gelten: Schreiben 25 %, Commit-p95 40 %, zufälliges Lesen-p95 20 %, Checkpoint 15 %. Die API-Kategorie misst die mediane Antwortzeit der laufenden HA-Instanz. Light verwendet 6 MiB/100 Commits/1.000 Reads; Full 64 MiB/1.000 Commits/5.000 Reads.</p><h2>Berechnung nach Kalibrierung</h2><p>Durchsatzindex = 100 × Messwert / Green-Referenz. Latenzindex = 100 × Green-Referenz / Messwert. Der Smartdomo Index ist das gewichtete geometrische Mittel. Temperatur, Energie und Linux PSI bleiben reine Diagnosewerte.</p><h2>Vergleichbarkeit</h2><p>Methodik-ID: CORE-2026.9.1-R4. Erst nach einer stabilen Green-Kalibrierung erhalten R4-Läufe Indizes und können in einer eigenen R4-Rangliste veröffentlicht werden. Die bestehende R3-Rangliste bleibt unverändert.</p>`;
const en=`<h1>Benchmark methodology R4</h1><h2>Status</h2><p>R4 fundamentally changes the workloads and is therefore not comparable with R3. Until new Green reference series exist for Light and Full, the app displays raw measurements only. R3 values are deliberately neither converted nor reused.</p><h2>Revision goal</h2><p>R4 still measures defined performance rather than general suitability. Workloads now represent installations with hundreds of entities and combine recurring with new data. Pure Python cache hits therefore have substantially less influence. Device price, installed-base statistics and suitability classes are outside R4.</p><h2>Planned weights</h2>${weights}<p>These weights will only be used after successful calibration.</p><h2>Core events</h2><p>Eight event types with varying payloads are generated and fully processed in bounded bursts. Light uses 24,000 and Full 120,000 events. Creation and processing are both inside the timed interval.</p><h2>State changes</h2><p>State changes are distributed across 400 entities in Light and 1,000 in Full. Every event receives new old and new Home Assistant State objects with varying values and attributes. Registered entity listeners are actually exercised, and bounded bursts limit memory use.</p><h2>Entity processing and JSON</h2><p>Entity processing combines include/exclude filtering and ID validation. 80% of IDs come from a recurring working set and 20% are new. Every JSON batch creates new State objects and attributes. Repeated serialization of one unchanged object is removed.</p><h2>Parallel Core load</h2><p>Up to two workers in Light and four in Full concurrently create and serialize fresh State objects in separate processes. Process startup and module import are outside the timed interval. This category captures multi-core capacity for concurrent HA-adjacent work and apps; it does not claim that the event-driven Core itself is multithreaded.</p><h2>Recorder storage and API</h2><p>The SQLite test remains WAL-based with synchronous=FULL. Inside storage: write throughput 25%, commit p95 40%, random-read p95 20%, checkpoint 15%. API measures median response time of the live HA instance. Light uses 6 MiB/100 commits/1,000 reads; Full uses 64 MiB/1,000 commits/5,000 reads.</p><h2>Calculation after calibration</h2><p>Throughput index = 100 × measured value / Green reference. Latency index = 100 × Green reference / measured value. The Smartdomo Index is the weighted geometric mean. Temperature, energy and Linux PSI remain diagnostic only.</p><h2>Comparison</h2><p>Methodology ID: CORE-2026.9.1-R4. R4 runs receive indices and become publishable in a separate R4 ranking only after stable Green calibration. The existing R3 ranking remains unchanged.</p>`;
function render(){document.documentElement.lang=language.value;document.getElementById('back').textContent=language.value==='de'?'Zurück':'Back';document.getElementById('method').innerHTML=language.value==='de'?de:en}language.onchange=()=>{localStorage.setItem('ha-benchmark-language',language.value);render()};render();
